---
phase: 08-frontend-build-nginx
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified: []
autonomous: false
requirements: [UI-01, UI-02]

must_haves:
  truths:
    - "Opening http://10.40.0.10/ in a browser loads the React dashboard (not the nginx default page)"
    - "All API calls from the browser reach the Express API via the nginx reverse proxy without CORS errors or 502s"
    - "The WebSocket connection to ws://10.40.0.10/ws connects and delivers real-time events"
    - "curl http://10.40.0.10/ returns HTTP 200 and the response body contains <script"
    - "curl http://10.40.0.10/health returns HTTP 200 (nginx proxying /health to :3000)"
  artifacts:
    - path: "/var/www/linbo/index.html"
      provides: "React SPA entry point on test server"
      contains: "<script"
    - path: "/etc/nginx/sites-available/linbo"
      provides: "nginx site config on test server"
      contains: "root /var/www/linbo"
    - path: "/etc/nginx/sites-enabled/linbo"
      provides: "symlink activating the linbo site"
  key_links:
    - from: "browser"
      to: "/var/www/linbo/index.html"
      via: "nginx try_files $uri $uri/ /index.html"
      pattern: "try_files"
    - from: "browser /api/"
      to: "localhost:3000/api/"
      via: "nginx proxy_pass"
      pattern: "proxy_pass http://localhost:3000"
    - from: "browser /ws"
      to: "localhost:3000/ws"
      via: "nginx proxy WebSocket upgrade"
      pattern: "Upgrade.*websocket"
---

<objective>
Deploy the pre-built frontend dist/ to test server 10.40.0.10 and verify the complete stack: static files, API proxy, WebSocket upgrade. This plan contains one automated deploy task followed by a mandatory human-verify checkpoint.

Purpose: Phases 1-7 built the native server stack — this plan makes it visible to an admin in a browser.
Output: Running frontend at http://10.40.0.10/, API and WebSocket proxied through nginx.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-frontend-build-nginx/08-RESEARCH.md
@.planning/phases/08-frontend-build-nginx/08-01-SUMMARY.md

<interfaces>
<!-- Test server connectivity -->
Server: 10.40.0.10
nginx: active (already installed, port 80 listening)
linbo-api.service: active on :3000
Default site: /etc/nginx/sites-enabled/default (must be removed)

<!-- Deploy script (created in Plan 01) -->
Usage: sudo bash scripts/server/deploy-frontend.sh
What it does:
  1. mkdir -p /var/www/linbo
  2. cp -r frontend/dist/. /var/www/linbo/ + chown www-data
  3. cp config/nginx.conf /etc/nginx/sites-available/linbo + symlink + remove default
  4. nginx -t && systemctl reload nginx
  5. curl smoke test: http://localhost/ must return 200

<!-- dist/ already built locally at -->
frontend/dist/index.html
frontend/dist/assets/index-Bxy8gA6a.css
frontend/dist/assets/index-DaPJKNYU.js
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync dist/ to test server and run deploy-frontend.sh</name>
  <files></files>
  <read_first>.planning/phases/08-frontend-build-nginx/08-01-SUMMARY.md</read_first>
  <action>
    Run the following sequence from the local repo root. Do NOT run npm install or npm run build on the server.

    Step 1 — Verify the local dist/ is present and contains index.html:
    ```bash
    test -f /root/linb-amo/frontend/dist/index.html && echo "dist OK"
    ```
    If missing, stop and report — do not proceed.

    Step 2 — Rsync the repo (including the newly-created deploy script and fixed nginx.conf) to the test server. Use the same rsync pattern established in prior phases. Exclude node_modules:
    ```bash
    rsync -avz --exclude='node_modules' --exclude='.git' \
      /root/linb-amo/ root@10.40.0.10:/root/linb-amo/
    ```

    Step 3 — Run the deploy script on the test server as root:
    ```bash
    ssh root@10.40.0.10 "cd /root/linb-amo && bash scripts/server/deploy-frontend.sh"
    ```

    Expected output: All 5 steps log OK, final line shows HTTP 200 smoke test passed.
    If nginx -t fails: stop, report the nginx error output.
    If curl returns non-200: stop, report — do not proceed to checkpoint.
  </action>
  <verify>
    <automated>ssh root@10.40.0.10 "curl -s -o /dev/null -w '%{http_code}' http://localhost/ && curl -s http://localhost/health | grep -c 'ok\|healthy\|status'"</automated>
  </verify>
  <acceptance_criteria>
    ssh root@10.40.0.10 "curl -s -o /dev/null -w '%{http_code}' http://localhost/" returns 200.
    ssh root@10.40.0.10 "curl -s http://localhost/ | grep -c '<script'" returns at least 1.
    ssh root@10.40.0.10 "test -f /var/www/linbo/index.html" exits 0.
    ssh root@10.40.0.10 "test -L /etc/nginx/sites-enabled/linbo" exits 0.
    ssh root@10.40.0.10 "test -e /etc/nginx/sites-enabled/default" exits 1 (default removed).
    ssh root@10.40.0.10 "grep 'root /var/www/linbo' /etc/nginx/sites-available/linbo" returns 1 match.
  </acceptance_criteria>
  <done>Frontend static files live at /var/www/linbo/, nginx serving from correct root, smoke test 200.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human browser verification of dashboard + WebSocket</name>
  <action>
    CHECKPOINT — pause for human verification. Task 1 automated deploy is complete. Human must confirm the browser experience before this plan is marked done.

    What was deployed: React frontend at http://10.40.0.10/, nginx proxying /api/ to :3000 and upgrading /ws WebSocket connections.
  </action>
  <verify>
    <automated>MISSING — visual browser verification required; automated smoke already passed in Task 1</automated>
  </verify>
  <done>Human confirms dashboard loads, API calls succeed, WebSocket stays connected at status 101.</done>

  <what-built>
    React frontend deployed at http://10.40.0.10/. nginx serves the Vite SPA, proxies /api/ to the Express API on :3000, and upgrades /ws WebSocket connections.
  </what-built>
  <how-to-verify>
    1. Open http://10.40.0.10/ in a browser.
       Expected: The LINBO dashboard / login page loads — NOT the nginx default "Welcome to nginx!" page.

    2. Open Browser DevTools > Network tab, reload the page.
       Expected: No 404 errors, no CORS errors, no 502 Bad Gateway responses on /api/ calls.

    3. Check WebSocket connection in DevTools > Network > WS filter.
       Expected: A connection to ws://10.40.0.10/ws appears with status 101 (Switching Protocols) and stays connected.

    4. Navigate between dashboard views (if login is required, log in first).
       Expected: React routing works without 404 on page reload — SPA fallback is active.

    5. Run from your terminal:
       curl -s http://10.40.0.10/health
       Expected: JSON response with status ok/healthy (not nginx 502 or connection refused).
  </how-to-verify>
  <resume-signal>Type "approved" when the dashboard loads and the WebSocket connects. Or describe what failed.</resume-signal>
</task>

</tasks>

<verification>
Automated (run from local machine before checkpoint):
- ssh root@10.40.0.10 "curl -s -o /dev/null -w '%{http_code}' http://localhost/" → 200
- ssh root@10.40.0.10 "curl -s http://localhost/ | grep -c '<script'" → >= 1
- ssh root@10.40.0.10 "nginx -t" → ok
- ssh root@10.40.0.10 "systemctl is-active nginx" → active
- ssh root@10.40.0.10 "test -L /etc/nginx/sites-enabled/linbo && echo linked" → linked
- ssh root@10.40.0.10 "test ! -e /etc/nginx/sites-enabled/default && echo removed" → removed

Manual (checkpoint):
- Browser: http://10.40.0.10/ loads React app
- DevTools Network: no CORS/502 errors
- DevTools WS: /ws connection status 101
</verification>

<success_criteria>
1. curl http://10.40.0.10/ returns 200 with React HTML body (contains script tag).
2. curl http://10.40.0.10/health returns 200 with API health JSON.
3. Browser loads the LINBO dashboard at http://10.40.0.10/.
4. WebSocket connection to /ws stays connected (status 101).
5. No nginx default page, no CORS errors, no 502s in browser DevTools.
</success_criteria>

<output>
After completion, create .planning/phases/08-frontend-build-nginx/08-02-SUMMARY.md
</output>
