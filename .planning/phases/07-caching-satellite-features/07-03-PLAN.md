---
phase: 07-caching-satellite-features
plan: 03
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
files_modified: []
autonomous: false
requirements:
  - CACHE-01
  - CACHE-02
  - CACHE-03
  - CACHE-04

must_haves:
  truths:
    - "linbo-api.service is active on 10.40.0.10"
    - "Multi-school sync completes without error (CACHE-01)"
    - "Host status worker is running and shows at least one host probe (CACHE-03)"
    - "First-boot sync fired on initial install — sync:cursor is set in store.json (CACHE-04)"
    - "Image pull endpoint accepts requests and either downloads or reports no Authority URL configured (CACHE-02)"
  artifacts:
    - path: "/var/lib/linbo-native/store.json"
      provides: "Persisted store state after first-boot sync"
      contains: "sync:cursor"
  key_links:
    - from: "GET /api/v1/sync/status"
      to: "store key sync:cursor"
      via: "Returns lastSyncAt and cursor from store"
      pattern: "sync:cursor"
---

<objective>
Deploy Phase 7 fixes to the test server (10.40.0.10) and manually verify all four
Caching-Satellite features work end-to-end on real hardware.

Purpose: Phase 7 is only done when it works on a real server — not just in unit tests.
Output: Verified working deployment on 10.40.0.10, human confirmation of all four features.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-caching-satellite-features/07-01-SUMMARY.md
@.planning/phases/07-caching-satellite-features/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy to test server 10.40.0.10</name>
  <files></files>
  <action>
    Deploy the Phase 7 code changes (sync.service.js fix + test path fixes) to the test server.
    The test server address is 10.40.0.10 (per STATE.md Phase 03-03 decision).

    Run these steps:
    1. On the test server, pull latest code:
       ssh root@10.40.0.10 "cd /srv/linbo-api && git pull origin main"

    2. Restart the API service:
       ssh root@10.40.0.10 "systemctl restart linbo-api.service && sleep 3"

    3. Verify service is active:
       ssh root@10.40.0.10 "systemctl is-active linbo-api.service"
       Expected output: "active"

    4. Check for startup errors:
       ssh root@10.40.0.10 "journalctl -u linbo-api.service --since '1 minute ago' --no-pager -n 20"
       Look for: "First boot detected" OR "Initial sync complete" OR normal startup messages.
       Fail if: any "Error:", "FATAL:", "Cannot find module" in output.

    5. Check store state:
       ssh root@10.40.0.10 "cat /var/lib/linbo-native/store.json 2>/dev/null | python3 -c \"import sys,json; d=json.load(sys.stdin); print('cursor:', d.get('strings',{}).get('sync:cursor','(absent)'))\" || echo 'store.json not found'"
  </action>
  <verify>
    <automated>ssh root@10.40.0.10 "systemctl is-active linbo-api.service" 2>/dev/null || echo "SKIP: no test server access"</automated>
  </verify>
  <done>
    - linbo-api.service reports "active" on 10.40.0.10
    - No module resolution errors in journal
    - store.json exists at /var/lib/linbo-native/store.json (may be empty if SYNC_ENABLED=false)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all four CACHE features on 10.40.0.10</name>
  <action>
    Verify all four Caching-Satellite features on the test server. Run these checks:

    CACHE-01 — Multi-school sync:
    Trigger a manual sync and watch logs:
      curl -s -X POST http://10.40.0.10:3000/api/v1/sync/trigger \
        -H "Authorization: Bearer YOUR_JWT" | python3 -m json.tool
    Expected: { "success": true, "stats": { ... } }
    In journal, look for: "[Sync] Starting ... sync (cursor: ..., school: YOUR_SCHOOL)"
      journalctl -u linbo-api.service -f --no-pager -n 30

    CACHE-02 — Image caching:
    Check image pull endpoint responds (no crash):
      curl -s -X POST http://10.40.0.10:3000/api/v1/sync/images/pull \
        -H "Authorization: Bearer YOUR_JWT" \
        -H "Content-Type: application/json" \
        -d '{"imageName":"ubuntu2404"}' | python3 -m json.tool
    Expected: either { "jobId": "..." } (Authority configured) or clean { "error": "..." } — NOT a 500 crash

    CACHE-03 — Auto-discovery (host status worker):
    Check worker is running:
      journalctl -u linbo-api.service --no-pager -n 50 | grep -i "host status\|worker"
    Expected: "Host Status Worker started" in startup log

    CACHE-04 — First-boot sync:
    Check store for cursor:
      ssh root@10.40.0.10 "cat /var/lib/linbo-native/store.json | python3 -c \
        'import sys,json; d=json.load(sys.stdin); print(d.get(\"strings\",{}).get(\"sync:cursor\",\"(absent)\"))'"
    If SYNC_ENABLED=true: cursor is a non-empty string like "1708943200:42"
    If SYNC_ENABLED=false: cursor absent — first-boot sync intentionally skipped (correct)

    Unit tests (run locally):
      npm test -- --testPathPattern="sync.service.school|startup-first-boot|sync.service.test|image-sync.service" --runInBand
    Expected: all suites pass, zero "Cannot find module" errors
  </action>
  <verify>
    Human confirms all four features respond correctly per the checks above.
  </verify>
  <done>
    All four CACHE-0x features verified:
    - CACHE-01: sync trigger shows school in journal log
    - CACHE-02: image pull endpoint returns JSON (not a 500 crash)
    - CACHE-03: "Host Status Worker started" appears in journal
    - CACHE-04: store.json cursor present (SYNC_ENABLED=true) or SYNC_ENABLED=false confirmed
    - Unit tests for Phase 7 pass locally
    Human types "approved" to continue.
  </done>
</task>

</tasks>

<verification>
Phase 7 complete when:
1. All unit tests pass: `npm test -- --runInBand` exits 0
2. linbo-api.service active on 10.40.0.10
3. All four CACHE-0x features verified by human (see checkpoint task)
</verification>

<success_criteria>
- CACHE-01: sync with hostsChanged="all" passes school to getChanges — verified by unit test + manual sync log
- CACHE-02: /sync/images/pull endpoint responds (no crash) — verified by curl
- CACHE-03: Host Status Worker started message in journal — verified by journalctl
- CACHE-04: store.json contains sync:cursor after first boot (if SYNC_ENABLED=true) — verified by cat
- Human types "approved" at checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/07-caching-satellite-features/07-03-SUMMARY.md`
</output>
