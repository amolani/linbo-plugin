---
phase: 07-caching-satellite-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/sync.service.js
  - tests/api/services/sync.service.test.js
  - tests/api/services/image-sync.service.test.js
  - tests/api/routes/sync-read.test.js
  - tests/api/routes/settings.test.js
  - tests/api/routes/sync-operations.test.js
  - tests/api/routes/internal-findHostByIp.test.js
  - tests/api/routes/internal-sidecar.test.js
  - tests/api/routes/health.test.js
  - tests/api/routes/system.hooks.test.js
  - tests/api/routes/system.linbo-update.test.js
  - tests/api/startup-validation.test.js
autonomous: true
requirements:
  - CACHE-01

must_haves:
  truths:
    - "syncOnce() passes school to getChanges() even when hostsChanged contains 'all'"
    - "All test files in tests/api/services/ and tests/api/routes/ resolve src modules correctly"
    - "npm test -- --testPathPattern='sync.service' runs without module resolution errors"
  artifacts:
    - path: "src/services/sync.service.js"
      provides: "Bug fix: school param in hostsChanged='all' branch (line 151)"
      contains: "lmnClient.getChanges('', school)"
    - path: "tests/api/services/sync.service.test.js"
      provides: "Corrected require paths (../../../src)"
  key_links:
    - from: "sync.service.js hostsChanged='all' branch"
      to: "lmnClient.getChanges"
      via: "school param forwarded"
      pattern: "getChanges\\('', school\\)"
---

<objective>
Fix the one confirmed CACHE-01 bug: sync.service.js drops the school parameter when
hostsChanged contains "all" (line 151). Fix the pre-existing test infrastructure breakage:
25 test files in tests/api/services/ and tests/api/routes/ use `../../src` paths that
resolve to the non-existent tests/src/ instead of the project-root src/.

Purpose: Unblock the test suite so Phase 7 verification can actually run. The school-param
bug is the only real CACHE-01 code defect — all other features are already implemented.

Output: One-line source fix + corrected require/mock paths in 25 test files.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-caching-satellite-features/07-RESEARCH.md

<interfaces>
<!-- From src/lib/lmn-api-client.js — exported functions and their signatures -->
```javascript
// All six API methods accept school as final parameter:
getChanges(cursor: string, school?: string): Promise<Delta>
batchGetHosts(macs: string[], school: string): Promise<{ hosts: Host[] }>
batchGetStartConfs(ids: string[], school: string): Promise<{ startConfs: StartConf[] }>
batchGetConfigs(ids: string[], school: string): Promise<{ configs: Config[] }>
getIscDhcpConfig(school: string): Promise<string>
getGrubConfigs(school: string): Promise<{ configs: GrubConfig[] }>
```

<!-- Bug in src/services/sync.service.js at line 151 — school variable IS in scope at this point -->
<!-- school is declared at line 72: const school = await settingsService.get('lmn_school'); -->
```javascript
// BEFORE (drops school — bug):
const fullDelta = await lmnClient.getChanges('');

// AFTER (correct — school already in scope as const):
const fullDelta = await lmnClient.getChanges('', school);
```

<!-- Path correction pattern for ALL tests/api/services/ and tests/api/routes/ files: -->
<!-- tests/api/services/foo.test.js: ../../src  →  ../../../src  (add one ../) -->
<!-- tests/api/routes/foo.test.js:   ../../src  →  ../../../src  (add one ../) -->
<!-- tests/api/startup-validation.test.js: ../src  →  ../../src  (add one ../) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix school param bug in sync.service.js + fix test path shims</name>
  <files>
    src/services/sync.service.js,
    tests/api/services/sync.service.test.js,
    tests/api/services/image-sync.service.test.js,
    tests/api/services/settings.service.test.js,
    tests/api/services/sync-operations.service.test.js,
    tests/api/services/grub-generator.test.js,
    tests/api/services/grub-sync.test.js,
    tests/api/services/grub-theme.service.test.js,
    tests/api/services/hook.service.test.js,
    tests/api/services/kernel.service.test.js,
    tests/api/services/linbofs.service.test.js,
    tests/api/services/linbo-update.service.test.js,
    tests/api/services/ssh.service.test.js,
    tests/api/services/terminal.service.test.js,
    tests/api/services/wol.service.test.js,
    tests/api/services/drivers.service.test.js,
    tests/api/services/firmware.service.test.js,
    tests/api/services/firmware-catalog.test.js,
    tests/api/routes/sync-read.test.js,
    tests/api/routes/settings.test.js,
    tests/api/routes/sync-operations.test.js,
    tests/api/routes/internal-findHostByIp.test.js,
    tests/api/routes/internal-sidecar.test.js,
    tests/api/routes/health.test.js,
    tests/api/routes/system.hooks.test.js,
    tests/api/routes/system.linbo-update.test.js,
    tests/api/startup-validation.test.js
  </files>
  <behavior>
    - sync.service.js: getChanges('', school) is called when hostsChanged includes 'all' — school must equal whatever settingsService.get('lmn_school') returned
    - sync.service.test.js school-passthrough tests pass without "Cannot find module" errors
    - image-sync.service.test.js runs without "Cannot find module '../../src/lib/redis'" error
  </behavior>
  <action>
    Part A — Source fix (1 line):
    In src/services/sync.service.js at line 151, change:
      `const fullDelta = await lmnClient.getChanges('');`
    to:
      `const fullDelta = await lmnClient.getChanges('', school);`
    The variable `school` is already in scope (declared at line 72 via settingsService.get('lmn_school')).
    Do NOT change anything else in sync.service.js.

    Part B — Path shim fix (sed-style replacement across 26 test files):
    The problem: tests/api/services/ and tests/api/routes/ files use `../../src` in require() and
    jest.mock() calls. This resolves to tests/src/ which does not exist.
    The project root src/ is 3 levels up from tests/api/services/ and tests/api/routes/, so the
    correct relative path is `../../../src`.

    For every file in tests/api/services/*.test.js:
      Replace all occurrences of `'../../src/` with `'../../../src/`
      Replace all occurrences of `"../../src/` with `"../../../src/`
      (This covers both jest.mock() strings and require() calls in those files.)

    For every file in tests/api/routes/*.test.js:
      Same replacement: `../../src/` → `../../../src/`

    For tests/api/startup-validation.test.js:
      Replace `'../src/` with `'../../src/`
      (This file is one level higher in tests/api/ so needs one fewer `..`)

    Do NOT touch tests/api/lib/*.test.js — those already use ../../../src correctly.
    Do NOT touch tests/api/workers/*.test.js — those already use ../../../src correctly.
    Do NOT touch tests/api/integration/ — those use different path patterns.
    Do NOT touch tests/api/middleware/ — check first before touching.

    After ALL replacements, verify no `../../src` remains in services/ or routes/ tests:
      grep -rn "require.*'../../src/\|mock.*'../../src/" tests/api/services/ tests/api/routes/
    This grep must return zero lines.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="sync.service.test|image-sync.service" --runInBand 2>&1 | tail -15</automated>
  </verify>
  <done>
    - `grep -n "getChanges(''" src/services/sync.service.js` shows `getChanges('', school)` at line ~151
    - `npm test -- --testPathPattern="sync.service.test|image-sync.service" --runInBand` exits 0 with no "Cannot find module" errors
    - `grep -rn "require.*'../../src/" tests/api/services/ tests/api/routes/` returns zero matches
  </done>
</task>

</tasks>

<verification>
After task completes:
1. `grep "getChanges" src/services/sync.service.js` — confirm both calls pass school
2. `npm test -- --testPathPattern="sync|image-sync" --runInBand` — all suites load and run
3. `grep -rn "'../../src/" tests/api/services/ tests/api/routes/` — zero matches
</verification>

<success_criteria>
- src/services/sync.service.js line ~151: `lmnClient.getChanges('', school)` (school param present)
- No "Cannot find module '../../src/..." errors in any test run
- npm test for sync + image-sync test suites exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/07-caching-satellite-features/07-01-SUMMARY.md`
</output>
