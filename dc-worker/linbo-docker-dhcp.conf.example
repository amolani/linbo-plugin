# LINBO Docker DHCP Redirect Configuration
# Used by post-import hook: 50-linbo-docker-dhcp
#
# Copy to /etc/linbo-docker-dhcp.conf and adjust values.
# The {school} placeholder is replaced at runtime.

# IP address of the LINBO Docker host (TFTP server)
LINBO_DOCKER_IP=10.0.0.13

# School name (must match linuxmuster.net school config)
SCHOOL=default-school

# Delta CSV written by DC worker (lists Docker-managed hosts)
DELTA_CSV=/etc/linuxmuster/sophomorix/{school}/linbo-docker.devices.csv

# ISC DHCP devices file generated by linuxmuster-import-devices
DEVICES_DHCP=/etc/dhcp/devices/{school}.conf

# Log file for hook output
LOGFILE=/var/log/linuxmuster/linbo-docker-dhcp.log
